docker run -d   --name maksmebel   --network mynetwork -p 3001:3001 -v /var/images:/var/images  maksmebel


docker build -t nginx .

docker run -d \
  --name nginx \
  --network mynetwork \
  -p 443:443 \
  nginx